version: '3.5'
services:
  project:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./project:/project
    ports:
      - "3001:3000"
    environment:
      - HOST=0.0.0.0